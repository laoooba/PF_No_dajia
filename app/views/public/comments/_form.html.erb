<%= form_with(model:[post, comment], remote: true) do |f| %>
  <%= f.text_area :comment, rows:'5',placeholder: "コメントをここに", class:"form-control form-control-lg" %>
  <p class="pt-2">
    <%= f.submit "送信する" , class:"btn btn-outline-success"%>
  </p>
<% end %>